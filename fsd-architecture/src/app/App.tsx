import { useEffect } from 'react';

function App() {
  useEffect(() => {
    // const { data } = fetchData();
    // console.log(data);
  }, []);
  return <h1>Hi, FSD Architecture</h1>;
}

export default App;
